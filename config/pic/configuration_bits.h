/*
 * 1. Open the `docs/xc8_docs/pic_chipinfo.html` and select the required chip to get the configuration details.
 * 2. Use `make pic_xc8_docs` command to get the compiler documents under `docs/xc8_docs`.
 */